20191009

遵守两段锁协议的事务一定不会死锁。（        ）
错误

判断对错。在java的多态调用中，new的是哪一个类就是调用的哪个类的方法。
错
java多态有两种情况：重载和覆写
在覆写中，运用的是动态单分配，是根据new的类型确定对象，从而确定调用的方法；
在重载中，运用的是静态多分派，即根据静态类型确定对象，因此不是根据new的类型确定调用的方法 

关系规范化中的删除操作异常是指( ) 
不该删除的数据被删除

如果int x=20, y=5，则语句System.out.println(x+y +""+(x+y)+y);  的输出结果是（）
25255

以下哪一个正则表达式不能与字符串“https://www.tensorflow.org/”（不含引号）匹配？（）
https[://]www[.]tensorflow[.]org[/]

public void test() {
        int a = 10;
        System.out.println(a++ + a--);
    }

21

在数据库的三级模式结构中，描述数据库中全体数据的全局逻辑结构和特征的是（ ）。 

模式

 SQL 中，"DELETE FROM 表名"表示（ ） 
 从基本表中删除所有元组
 
不允许在关系中出现重复记录的约束是通过（ ）？
主关系键实现

所有的视图都可以更新。（         ）
错误

授权编译系统和合法性检查机制一起组成了（  ）子系统。 
安全性

实体是信息世界中的术语，与之对应的数据库术语为（ ）。 
记录

数据库事务的特性不包含：
并发性

A：原子性（atomicity）
C：一致性（consistency）
I：隔离性（isolation）
D：持久性（durability）

下面哪些字符可能会导致sql注入?
‘（单引号）
SQL注入的关键是单引号的闭合 

若有下列定义，下列哪个表达式返回false？
String s = "hello";
String t = "hello";
char c[] = {'h','e','l','l','o'} ; 
t.equals(c);

//String和char咋能相等

在下列描述中，正确的描述是（ ）。
SQL 采用集合操作方式

子模式DDL用来描述( )。
数据库的局部逻辑结构

假如某个JAVA进程的JVM参数配置如下：
-Xms1G -Xmx2G -Xmn500M -XX:MaxPermSize=64M -XX:+UseConcMarkSweepGC -XX:SurvivorRatio=3,
请问eden区最终分配的大小是多少？

300M

SQL 语言称为（）。 
结构化查询语言

下述那一条不属于概念模型应具备的性质（ ） 
在计算机中实现的效率高

关系规范化中的删除操作异常是指( ) 
不该删除的数据被删除

三个模式反映了对数据库的三种不同观点，以下说法中正确的是（　　）。
外模式表示了用户级数据库，体现了对数据库的用户观。

数据库管理系统是（） 
.一组软件

Java 中的集合类包括 ArrayList 、 LinkedList 、 HashMap 等，下列关于集合类描述正确的是？（）

ArrayList和LinkedList均实现了List接口
ArrayList访问速度比LinkedList快
HashMap实现Map接口，它允许任何类型的键和值对象，并允许将NULL用作键或值

若实体A和B是一对多的联系,实体B和C是一对一的联系,则实体A和C的联系是？
一对多

自然连接是构成新关系的有效方法。一般情况下，当对关系 R 和 S 使用自然连接时，要求 R 和 S 含有一个或多个共有的（ ）
属性

（ ）是数据库物理结构和存储方式的描述。.
存储模式

Mysql查询时，只有满足联接条件的记录才包含在查询结果，这种联接是（）。
内联接

视图是一个"虚表"，视图的构造基于（ ）? 
基本表或视图

在一个ER模型中，有3个不同的实体集和3个不同的二元联系，其中有1个一对一联系、1个 一对多联系和1个多对多联系，根据ER模型转换成关系模型的规则，转换成关系的数目是(  )。
4
假设实体是A、B、C，A与B是一对一，那么产生一个关系，A与C一对多,那么也产生一个关系，B与C多对多，则会产生一个中间表E,即关系(B,E)与(C,E)，所以答案是4 

ceil：大于等于 x，并且与它最接近的整数。
floor：小于等于 x，且与 x 最接近的整数。 

1NF 的二维表消除了传递函数依赖，则必定是（ ）？ 
1NF


